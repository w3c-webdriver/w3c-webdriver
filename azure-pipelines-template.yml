parameters:
  name: ''
  vmImage: ''
  nodeVersion: ''
  browser: ''

jobs:
- job: ${{ parameters.name }}
  pool:
    vmImage: ${{ parameters.vmImage }}
  variables:
    BROWSER: ${{ parameters.browser }}
  steps:
  - task: NodeTool@0
    inputs:
      versionSpec: ${{ parameters.nodeVersion }}
  - script: npm install
  - script: npm run lint
  - script: npm test
